### 1. Fetch all users
Goal: Understand `.all`

### 2. Fetch the first user
Goal: `.first` vs `.take`

### 3. Find user by email
Constraint: Use `.find_by`

### 4. Count total users
Try `.count`, `.length` and `.size`
Observe SQL difference.

### 5. Fetch last 5 users
Use `.order` and `.limit`
