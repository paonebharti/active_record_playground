### 1. List users and their post titles (N+1 warning)
Run without includes first.

### 2. Fix N+1 using `includes`

### 3. Use `joins` instead of `includes`
Observe difference in SQL.

### 4. When does `includes` generate a JOIN?
Experiment with `where` conditions.
